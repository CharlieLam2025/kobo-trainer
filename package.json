{
  "name": "kobo-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "口播练习器 · build pipeline (esbuild)",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

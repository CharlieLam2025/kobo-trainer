{
  "name": "kobo-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "口播练习器 · build pipeline (esbuild)",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "esbuild": "^0.24.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.17"
  }
}
